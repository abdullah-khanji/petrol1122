{
  "name": "petrol-pump-app",
  "version": "0.0.1",
  "main": "main.js",
  "scripts": {
  "dev": "concurrently -k \"cd renderer2 && pnpm dev\" \"cd backend && uvicorn app:app --reload --port 5000\" \"electron .\"",
  "build": "vite build && electron-builder"
},
  "dependencies": {
    "electron": "28.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "electron-builder": "^24.0.0",
    "vite": "^5.0.0"
  }
}